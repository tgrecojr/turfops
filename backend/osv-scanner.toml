# osv-scanner configuration
# Per https://google.github.io/osv-scanner/configuration/, this file applies
# only to lockfiles in the same directory (backend/Cargo.lock), which is
# exactly the scope we want.
#
# No active ignores. The previous RUSTSEC-2023-0071 (rsa Marvin Attack) ignore
# was removed once `rsa` dropped out of Cargo.lock entirely — `sqlx-mysql` no
# longer pulls it in, so there is nothing left to suppress.
